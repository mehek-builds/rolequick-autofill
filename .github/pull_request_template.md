## What this changes

<!-- One or two lines: the problem and the fix. -->

## Why

<!-- Context: what this unblocks or why it matters. -->

## Checklist

- [ ] `npm run compile` passes (type-check)
- [ ] `npm test` passes (Vitest)
- [ ] `npm run build` succeeds (wxt build)
- [ ] Loaded the built extension and verified the change end to end
